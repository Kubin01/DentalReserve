# render.yaml - 放在项目根目录
services:
  - type: web
    name: dentalreserve
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn dental_now:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16
